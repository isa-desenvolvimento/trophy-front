language: node_js
node_js:
  - "v14.15.3"
cache: yarn
before_script:
  - yarn global add vue-cli
  - sudo apt-get install expect
script:
  # test
  - yarn test:unit 
  - yarn run build
deploy:
  provider: pages
  local-dir: dist/
  github-token: 'bc773e539e2bc5eb9a228882aa32b7baf3a538ab'  
  keep-history: false
  on:
    branch: master
  target-branch: gh-pages
  committer-from-gh: true